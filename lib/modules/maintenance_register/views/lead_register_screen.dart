import 'package:flutter/material.dart';

class LeadRegisterScreen extends StatelessWidget {
  const LeadRegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
